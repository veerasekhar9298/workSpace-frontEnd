 const initialState  = {}

 const workSpaceReducer = (state = initialState,action) =>{

    switch (action.type){


        default :{
            return {...state}
        }
    }
 }



 export default workSpaceReducer

